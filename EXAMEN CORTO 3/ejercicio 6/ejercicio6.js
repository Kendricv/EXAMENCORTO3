//Ejercicio 6
	//Escribe una función que compruebe si un número dado tiene 3 dígitos y devuelva si o no.
    function tieneTresDigitos(num) {
        return num >= 100 && num <= 999;
        }
        console.log(tieneTresDigitos(123));